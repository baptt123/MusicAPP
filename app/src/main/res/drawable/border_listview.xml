<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@android:color/transparent" /> <!-- Màu nền trong suốt -->
    <stroke android:width="1dp" android:color="@android:color/black" /> <!-- Đường viền đen với độ dày 1dp -->
    <corners android:radius="5dp" /> <!-- Độ cong của góc (nếu cần) -->
</shape>
